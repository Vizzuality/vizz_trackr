<% content_for :title, "Editing Report - #{@report.reporting_period.display_name}" %>
<% content_for :actions do
  content_tag(:button, content_tag(:i, nil, class: 'fa fa-info'),
              data: { toggle: 'modal', target: '#reportsInfoModal' },
                class: 'btn btn-sm mr-3 btn-icon btn-icon-only btn-dark')
      .concat(link_to('Back', current_user, class: 'btn btn-sm btn-outline-primary'))
  end
%>
<% content_for :modals, render('non_project_reporting') %>

<%= render 'form', report: @report %>
