<% content_for :title, "Editing Project" %>
<% content_for :actions,
  link_to('Show', @project, class: 'btn btn-sm btn-outline-primary').concat(
    link_to('Back', projects_path, class: 'btn btn-sm btn-outline-secondary'))
%>

<%= render 'form', project: @project %>
