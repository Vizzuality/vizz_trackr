<% content_for :title, "New Invoice" %>
<% content_for :actions,
  link_to('Back', invoices_path, class: 'btn btn-sm btn-outline-secondary') %>

<%= render 'form', invoice: @invoice, contract: @contracts %>
