<% content_for :title "Editing Non Staff Cost" %>
<% content_for :actions,
  link_to('Show', @non_staff_cost, class: 'btn btn-sm btn-outline-primary').concat(
   link_to('Back', non_staff_costs_path, class: 'btn btn-sm btn-outline-secondary')) %>

<%= render 'form', non_staff_cost: @non_staff_cost %>
