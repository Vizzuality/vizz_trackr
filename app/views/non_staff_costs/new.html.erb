<% content_for :title, "New Non Staff Cost" %>
<% content_for :actions,
  link_to('Back', non_staff_costs_path, class: 'btn btn-sm btn-outline-primary') %>

<%= render 'form', non_staff_cost: @non_staff_cost %>
