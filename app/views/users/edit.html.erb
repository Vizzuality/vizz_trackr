<% content_for :title, "Editing User" %>
<% content_for :actions,
  link_to('Show', @user, class: 'btn btn-sm btn-outline-primary').concat(
  link_to('Back', users_path, class: 'btn btn-sm btn-outline-secondary'))%>

<%= render 'form', user: @user %>
