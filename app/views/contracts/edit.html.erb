<% content_for :title, "Editing Contract" %>
<% content_for :actions,
  link_to('Show', @contract, class: 'btn btn-sm btn-outline-primary').concat(
    link_to('Back', contracts_path, class: 'btn btn-sm btn-outline-secondary'))
%>

<%= render 'form', contract: @contract, projects: @projects %>
